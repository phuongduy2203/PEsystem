@{
    ViewData["Title"] = "Scrap Area";
    Layout = "~/Areas/Scrap/views/Shared/Layout_scrap.cshtml";
}

<div class="scrap-page">
    <div class="scrap-page-header">
        <h1 class="scrap-title">SN Wait SPE Approve</h1>
        <p class="scrap-subtitle">Theo dõi và nhập danh sách bản đang chờ SPE phê duyệt hoặc tải báo cáo nhanh.</p>
    </div>

    <div class="row g-4 align-items-start">
        <div class="col-lg-4">
            <div class="scrap-card selection-card">
                <h2 class="scrap-card-title">Chức năng nhanh</h2>
                <p class="scrap-card-text">Chọn thao tác để hiển thị đúng biểu mẫu xử lý.</p>
                <form id="search-form" class="scrap-selector">
                    <label for="search-options" class="form-label fw-semibold text-uppercase">Danh mục chức năng</label>
                    <select id="search-options" class="form-select">
                        <option selected disabled>SELECT FUNCTION</option>
                        <option value="INPUT_SN_1">INPUT SN</option>
                        <option value="SN_WAIT_SPE_APPROVE">SN WAIT SPE APPROVE</option>
                    </select>
                </form>
            </div>
        </div>
        <div class="col-lg-8">
            <div class="scrap-form-stack">
                <form id="input-sn-1-form" method="post" class="scrap-card scrap-form hidden" data-search-type="INPUT_SN">
                    <div class="scrap-form-header">
                        <h3>Nhập danh sách SN chờ SPE</h3>
                        <p>Ghi nhận bản đang gửi SPE phê duyệt cùng loại bonepile và trạng thái mong muốn.</p>
                    </div>
                    <div class="row g-4">
                        <div class="col-md-4">
                            <label for="sn-input-1" class="form-label">Danh sách Serial Number</label>
                            <textarea name="serialNumbers" id="sn-input-1" class="form-control" rows="8" placeholder="Nhập mỗi SN trên một dòng"></textarea>
                        </div>
                        <div class="col-md-4">
                            <div class="mb-3">
                                <label for="description-input-1" class="form-label">Mô tả</label>
                                <input type="text" id="description-input-1" name="description" class="form-control" placeholder="Nhập mô tả chi tiết">
                            </div>
                            <div class="mb-3">
                                <label for="bp-options" class="form-label">Type Bonepile</label>
                                <select id="bp-options" class="form-select">
                                    <option selected disabled>SELECT TYPE BONEPILE</option>
                                    <option value="BP-10">BONEPILE 1.0</option>
                                    <option value="BP-20">BONEPILE 2.0</option>
                                    <option value="B36R">AFFTER KANBAN</option>
                                </select>
                            </div>
                            <div class="text-end">
                                <button id="input-sn-btn" class="btn btn-ne btn-gradient" type="button">
                                    <i class="fas fa-upload me-2"></i>INPUT SN
                                </button>
                            </div>
                        </div>
                        <div class="col-md-4">
                            <label for="approve-options" class="form-label">Trạng thái yêu cầu</label>
                            <select id="approve-options" class="form-select">
                                <option selected disabled>SELECT TYPE APPROVE</option>
                                <option value="2">Waiting SPE Approve Scrap</option>
                                <option value="4">Waiting SPE Approve BGA</option>
                            </select>
                        </div>
                    </div>
                </form>

                <form id="custom-form" method="post" class="scrap-card scrap-form hidden" data-search-type="SN_WAIT_LIST_FORM">
                    <div class="scrap-form-header">
                        <h3>Danh sách chờ SPE approve</h3>
                        <p>Tải dữ liệu tổng hợp các bản đang trong trạng thái chờ phê duyệt.</p>
                    </div>
                    <div class="d-flex justify-content-end">
                        <button id="sn-wait-list-btn" class="btn btn-ne btn-gradient" type="button">
                            <i class="fas fa-file-download me-2"></i>Download Excel
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <div class="scrap-results">
        <div id="input-sn-1-result" class="scrap-card scrap-result hidden"></div>

        <div id="sn-wait-approve-result" class="scrap-card scrap-result hidden">
            <div class="table-responsive">
                <table id="table-sn-wait-approve" class="table table-hover align-middle" style="width:100%">
                    <thead>
                        <tr>
                            <th>SERIAL_NUMBER</th>
                            <th>DESCRIPTION</th>
                            <th>CREATE_TIME</th>
                            <th>APPLY_TASK_STATUS</th>
                            <th>BONEPILE_TYPE</th>
                            <th>CREATE_BY</th>
                        </tr>
                    </thead>
                    <tbody></tbody>
                </table>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <style>
        .hidden {
            display: none !important;
        }

        .scrap-page {
            background: linear-gradient(135deg, rgba(118, 185, 0, 0.12), rgba(118, 185, 0, 0.05));
            border-radius: 24px;
            padding: 2.5rem;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.08);
        }

        .scrap-page-header {
            text-align: center;
            margin-bottom: 2rem;
        }

        .scrap-title {
            font-size: 2rem;
            font-weight: 700;
            color: #1A2E05;
        }

        .scrap-subtitle {
            color: #3f4d2a;
            max-width: 680px;
            margin: 0 auto;
        }

        .scrap-card {
            background: #ffffff;
            border-radius: 20px;
            padding: 1.75rem;
            box-shadow: 0 12px 30px rgba(26, 46, 5, 0.08);
            border: 1px solid rgba(118, 185, 0, 0.18);
        }

        .scrap-card-title {
            font-weight: 700;
            color: #1A2E05;
        }

        .scrap-card-text {
            color: #4f5b39;
            margin-bottom: 1.25rem;
        }

        .scrap-form-header h3 {
            font-weight: 700;
            color: #1A2E05;
        }

        .scrap-form-header p {
            color: #4f5b39;
            margin-bottom: 1.25rem;
        }

        .scrap-form-stack .scrap-card + .scrap-card,
        .scrap-results .scrap-card + .scrap-card {
            margin-top: 1.5rem;
        }

        .btn-gradient {
            background: linear-gradient(135deg, #76B900, #4E8C00);
            border: none;
            color: #fff !important;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            transition: transform 0.2s ease, box-shadow 0.2s ease;
        }

            .btn-gradient:hover {
                transform: translateY(-2px);
                box-shadow: 0 10px 18px rgba(118, 185, 0, 0.35);
            }

        .table thead th {
            background-color: #76B900;
            color: #fff;
            font-weight: 700;
            text-transform: uppercase;
            border: none;
        }

        .table tbody tr:hover {
            background-color: rgba(118, 185, 0, 0.1);
        }

        .form-select:focus, .form-control:focus {
            border-color: #76B900;
            box-shadow: 0 0 0 0.2rem rgba(118, 185, 0, 0.25);
        }

        textarea.form-control {
            resize: vertical;
            min-height: 180px;
        }


            .scrap-form-stack .scrap-card + .scrap-card {
                margin-top: 1rem;
            }
        }
    </style>
    <script src="~/assets/areas/scrap/js/function1.js?v=@DateTime.Now.Ticks"></script>
}
